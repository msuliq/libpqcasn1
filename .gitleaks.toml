# Gitleaks configuration for libpqcasn1
# Excludes test fixtures from secret scanning

[allowlist]
  # Test file contains intentional cryptographic test vectors, not real secrets
  files = [
    "test/test_pqc_asn1\\.c"
  ]
